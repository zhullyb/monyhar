<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
  "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<!--
  Copyright 2019 The Monyhar Authors. All rights reserved.
  Use of this source code is governed by a BSD-style license that can be
  found in the LICENSE file.
-->
<busconfig>
  <!-- Allow the Chrome browser process to run this service. -->
  <policy user="chronos">
    <allow own="org.monyhar.CryptohomeKeyDelegate"/>
  </policy>
  <!-- Allow Tast integration tests (running as root) to run this service. -->
  <policy user="root">
    <allow own="org.monyhar.TestingCryptohomeKeyDelegate"/>
  </policy>

  <!-- Allow the cryptohomed daemon (running as root) to use this service. -->
  <policy user="root">
    <allow send_destination="org.monyhar.CryptohomeKeyDelegate"
           send_interface="org.monyhar.CryptohomeKeyDelegateInterface" />
    <allow send_destination="org.monyhar.TestingCryptohomeKeyDelegate"
           send_interface="org.monyhar.CryptohomeKeyDelegateInterface" />
  </policy>
</busconfig>
